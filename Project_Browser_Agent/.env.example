# Copy to `.env` and edit. Set the model ONCE; all four scripts read it.
# Ollama needs the FULL tag, including the size: `qwen3` and `qwen3:1.7b`
# are different names to the server.
model = qwen3:1.7b

# host = http://localhost:11434
